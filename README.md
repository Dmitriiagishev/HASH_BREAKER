# ТОИБАС Домашнее Задание
## Условие задачи
Восстановить пароли по известным криптографическим хэшам. Про восстанавливаемые пароли известны их длина и алфавит. Пароли не словарные. Время восстановления паролей к файлу из 100 хэшей должно быть не больше 1 часа. Файл с хэшами для проверки выполнения задания высылается после получения уведомления о готовности вас к приемке этапа 1. Это текстовый файл, где каждая строка – хэш пароля. Не позднее 1 часа должен быть прислан файл с паролями, причём должно быть вскрыто не менее 50% паролей.
Выполнил - Агишев Д, ИУ8-71
Длина пароля - 9 символов. 
Допустимые символы: 2468*WuiopZbvcs (15 различных символов).
Хэш-функция MD5(SHA1(SHA-256("someSalt2"+value+"Additional890"))).
# Brute-Force
Изначально планировалось решение при помощи brute-force перебора всех возможных вариантов пароля.
Все пространство паролей разбивалось на столько частей (чанков), сколько потоков поддерживает процессор компьютера, решающего задачу. Затем, по каждому из них начинался перебор всех возможных паролей из этого чанка с последующим сравнением их хэшей с содержимым входного файла.
Программа была реализована на языке python, но, ввиду плачевной производительности, было принято решение перенести логику на язык c++.
К сожалению, даже при переходе на более производительныый язык, мощностей моего железа не хватало, и ноутбук с intel core i3, в среднем, находил ~14 паролей из 100 (тестовая выборка была случайно сгенерирована) при использовани 4 потоков для вычислений, а ноутбук с intel core i7 ~13 паролей из 100 на 8 потоках.
В теории, все пространство паролей можно разделить на 12 частей и 4 из них обрабатывать на одном ноутбуке, и 8 на другом, но, было обговорено, что, хоть задачу можно решить любым способом, подразумевалось решение с использованием радужных таблиц, так что, я не стал испывать этот метод (хотя, все еще хочу опробовать разделить вычисления на 2 машины).
# Rainbow table
## Rainbow_table_generator
Далее, для решения задачи было принято составить и использовать радужную таблицу. 
Т.к. всего возможно 15^9 (~38.4 млрд) вариантов паролей, необходимо, чтобы <длина_цепочки>*<количество_цепочек> >= 38.4 * 10^9, чтобы, в теории, вся таблица покрывала все возможные пароли (Хотя, на практике выяснилось, что итоговая таблица покрывает ~ 62% от всех возможных паролей с заданными условиями).
Для разумного компромисса между скоростью вычислений и занимаемой памятью, были выбраны num_chains = 38.450.000 (Среди этих цепочек нет слипшихся, но, потом их количество было дополнительно немного дополнено) и chain_length = 1000 (Хотя, как оказалось во время напичания кода для поиска, из-за особенностей реализации, 999).
Программа rainbow_table_generator.cpp, реализующая эту задачу, была написана на языке c++.
Итоговая таблица имеет размер около 1.5 Гб и считалась на протяжении 31 часа.
## Hash_breaker
Далее, для поиска хэш функций по готовой таблице, была написана программа hash_breaker.cpp. Она принимает на вход два фала: rainbow_table.txt (получаемый при работе rainbow_table_generator) и hashes.txt (в котором записаны хэш функции, по которым необходимо восстановить пароли). Далее, программа начинает искать хэш-функции в данной таблице и, при нахождении соответствия хэш-функция -> пароль, выводит в консоль строку в формате 

<найденый пароль>  <хэш функция из данного файла>  <заново посчитанная хэш-функция для данного пароля>

Хэш-функция еще раз считается для пароля, т.к. из-за ограниченных возможнстей функции редукции, достаточно много разных хэшей переходят в одинаковый пароль при ее использовании, что может вызывать ложно-положителььные срабатывания.

### Ход работы
1. Изначально, данные из файла с радужной таблицей считываются в словарь (std::unordered_map, ключ - последний хэш в цепочке, значение - первый пароль) для быстрого определени наличия в нем определенной хэш-функции и, в случае существования такого элеменета, получения головы этой цепочки; Данные из файл с хэш-функциями записываются в простой динамический массив (std::vector).
2. Далее, проверяется, не является ли какой-либо из данных хэшей концом какой-либо цепочки. В таком случае, зная начальный пароль в цепи, цепочка легко воспроизводится, благода чему,находятся изначальные пароли. Хэш функции, по которым восстанавливаются пароли на этой итерации "убираются" из общего списка.
3. Далее, предполагается, что все элементы, оставшиеся в массиве после предыдущего шага, могут быть предпоследними элементами цепи. для каждого элмента строится фрагмент цепи из двух элементов, после чего, концы этих элементов сравниваются с концами изнаальной таблице. Если есть совпадениия - данные элементы убираются из массива.
4. Далее, для всех оставшихся элементов наинается цикл, в котором, с каждой итерацией предполагается, что элемент находится все дальше и дальше от конца цепочки. С каждой итерацией, найденные элементы удаляются из списка.
5. Из-за особенностей реализации, элементы находятся некорректно, если они являются вторыми элементами в таблице, для чего, организуется цикл, просматриваюий начала цепочек и для каждой находится хэш функция и сравнивается с оставшимися.
### Итог
В среднем, при использовании данной фукции, вся таблица просматривается за ~ 10 минут, находя около 62 паролей из тестовой выборки, размером в 100 паролей, что, хоть и менее надежно, по сравнению с brute-force, но, имеет колоссальную разницу в скорости вычисления.  
Созданная реализация не идеальна и является, скорее, proof-of-concept, нежели полноценным инструментом. Хотя, в данной стадии, она исправно выполняет свою задачу, есть много вариатов, как ее улучшить: от увеличения количества цепей в таблице, до доработки функцииций в hash_breaker. Но, к сожалению, это не единственное задание, требующее выполнения, из-за чего, эти доработки откладываются в долгий ящик, но, при необходимости, я улучшу свою разраотку.

